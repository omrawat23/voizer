export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Voizer",
  description:
    "Voizer component with server side sorting, pagination, and filtering",
  mainNav: [
    {
      title: "",
      href: "/",
    }
  ],
}
